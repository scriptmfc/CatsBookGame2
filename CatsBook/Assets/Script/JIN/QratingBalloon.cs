using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QratingBalloon : MonoBehaviour
{
     public List<Book> QratingBook_List = new List<Book>();
     public GameObject HostCat;

     public Vector3 offset;

    // Update is called once per frame
    void Update()
    {
          //if(HostCat!=null)
          //transform.position = HostCat.transform.position + offset;
    }
}
